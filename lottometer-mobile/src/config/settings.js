const settings = {
  // Flip to true when offline mode is ready
  OFFLINE_MODE_ENABLED: false,

  // Offline session expiry in hours
  OFFLINE_SESSION_EXPIRY_HOURS: 72,

  // Max days of data to keep offline
  OFFLINE_DATA_DAYS: 7,

  // Max PIN attempts before lockout
  OFFLINE_MAX_PIN_ATTEMPTS: 5,
};

export default settings;
